package InterviewPrograms;
// here we have one array and we need to find out the frequency of duplicates using map

// arr = {1, 1,1 ,2 , 2, 3, ,3 ,3, 3}

// here we have to create a program that will show the frequency of each numbers in this


import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.Random;

public class FindFrequency {


    public static void main(String[] args) {
//        DateTimeFormatter df = DateTimeFormatter.ofPattern("yyyy/MM/dd HH:mm:ss");
//        LocalDateTime now =LocalDateTime.now();
//        System.out.println(df.format(now));

        // java program to find out the random number

        Random rand = new Random();
        int r1 = rand.nextInt(1000);

        // This approach we can use to find out the random number in java

        

    }

}
